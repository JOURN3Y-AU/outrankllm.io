/**
 * HiringBrand Account — Server Component
 * Auth is handled by middleware, this just renders the client component.
 */

import { DashboardClient } from './DashboardClient'

export default function HiringBrandAccountPage() {
  return <DashboardClient />
}
